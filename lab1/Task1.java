package lab1;
import java.util.Arrays;
import java.util.ArrayList;
import java.util.List;
import java.util.Collections;

public class Task1 {
    // Default main method
    // public static void main(String[] args) {
    //     // Define an array of numbers
    //     int[] numbers = { 1, 2, 3, 4, 5 };
    //     // Iterate over the array and print each number
    //     for (int i = 0; i < numbers.length; i++) {
    //         System.out.println(numbers[i]);
    //     }
    // }

    // Summing up all the numbers in the array.
    // public static void main(String[] args) {
    //     // Define an array of numbers
    //     int[] numbers = { 1, 2, 3, 4, 5 };
    //     // Define a variable to hold the sum
    //     int sum = 0;
    //     // Iterate over the array and add each number to sum
    //     for (int i = 0; i < numbers.length; i++) {
    //         sum += numbers[i];
    //     }
    //     // Print the sum
    //     System.out.println("The sum is: " + sum);
    // }

    // Finding the maximum or minimum value in the array.
    // public static void main(String[] args) {
    //     // Define an array of numbers
    //     int[] numbers = { 1, 2, 3, 4, 5 };

    //     // Define variables to hold the maximum and minimum
    //     int max = numbers[0];
    //     int min = numbers[0];

    //     // Iterate over the array
    //     for (int i = 1; i < numbers.length; i++) {
    //         // If the current number is greater than max, update max
    //         if (numbers[i] > max) {
    //             max = numbers[i];
    //         }
    //         // If the current number is less than min, update min
    //         if (numbers[i] < min) {
    //             min = numbers[i];
    //         }
    //     }

    //     // Print the maximum and minimum
    //     System.out.println("The maximum is: " + max);
    //     System.out.println("The minimum is: " + min);
    // }

    // Sorting the array in ascending or descending order.
    // public static void main(String[] args) {
    //     // Define an array of numbers
    //     Integer[] numbers = { 1, 2, 3, 4, 5 };

    //     // Sort the array in ascending order
    //     Arrays.sort(numbers);
    //     System.out.println("Ascending order: " + Arrays.toString(numbers));

    //     // Sort the array in descending order
    //     Arrays.sort(numbers, Collections.reverseOrder());
    //     System.out.println("Descending order: " + Arrays.toString(numbers));
    // }

    // Generating a new array with squared values of the original numbers.
    // public static void main(String[] args) {
    //     // Define an array of numbers
    //     Integer[] numbers = { 1, 2, 3, 4, 5 };

    //     // Create a new array to hold the squared numbers
    //     Integer[] squaredNumbers = new Integer[numbers.length];

    //     // Iterate over the original array
    //     for (int i = 0; i < numbers.length; i++) {
    //         // Square each number and store it in the new array
    //         squaredNumbers[i] = numbers[i] * numbers[i];
    //     }

    //     // Print the new array
    //     System.out.println("Squared numbers: " + Arrays.toString(squaredNumbers));
    // }

    // Filtering the array to only include even or odd numbers.
    // public static void main(String[] args) {
    //     // Define an array of numbers
    //     Integer[] numbers = { 1, 2, 3, 4, 5 };

    //     // Create a new list to hold the even numbers
    //     List<Integer> evenNumbers = new ArrayList<>();

    //     // Create a new list to hold the odd numbers
    //     List<Integer> oddNumbers = new ArrayList<>();

    //     // Iterate over the original array
    //     for (int number : numbers) {
    //         // If the number is even, add it to the even numbers list
    //         if (number % 2 == 0) {
    //             evenNumbers.add(number);
    //         }
    //         // If the number is odd, add it to the odd numbers list
    //         else {
    //             oddNumbers.add(number);
    //         }
    //     }

    //     // Print the even and odd numbers
    //     System.out.println("Even numbers: " + evenNumbers);
    //     System.out.println("Odd numbers: " + oddNumbers);
    // }
}


